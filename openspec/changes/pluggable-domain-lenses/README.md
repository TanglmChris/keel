# pluggable-domain-lenses

Make Keel's domain knowledge pluggable: the core keeps the detect-and-load mechanism, while lens content moves out of the shipped skills into user-authored `keel/lenses/*.md`.
